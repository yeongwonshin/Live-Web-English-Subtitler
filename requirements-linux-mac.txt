faster-whisper>=1.0.3
numpy>=1.24
sounddevice>=0.4.6
# Optional but recommended on macOS: makes the subtitle overlay stay above normal windows and join Spaces.
pyobjc-framework-Cocoa>=10.0; platform_system == "Darwin"
